"use client";

import MaturityPage from "../../maturity/page";

/** Lovable route: `/control-tower/maturity` — same experience as Maturity Engine. */
export default function ControlTowerMaturityPage() {
  return <MaturityPage />;
}
